import 'package:flutter/material.dart';

class MathematicScreen extends StatelessWidget {
  const MathematicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Mathematic screen'),
      ),
    );
  }
}