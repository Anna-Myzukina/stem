import 'package:flutter/material.dart';

class CoverLaterGeneratorScreen extends StatelessWidget {
  const CoverLaterGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Cover Letter Generator'),
      ),
    );
  }
}