import 'package:flutter/material.dart';

class CvGeneratorScreen extends StatelessWidget {
  const CvGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('CV Generator soon be here'),
      ),
    );
  }
}