import 'package:flutter/material.dart';

class EnvironmentalScienceScreen extends StatelessWidget {
  const EnvironmentalScienceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Environmental Screen'),
      ),
    );
  }
}