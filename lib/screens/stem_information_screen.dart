import 'package:flutter/material.dart';

class StemInformationScreen extends StatelessWidget {
  const StemInformationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('STEM Information'),
      ),
    );
  }
}